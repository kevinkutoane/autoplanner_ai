org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Suppress the "Inconsistent JVM-target compatibility" hard error introduced in Kotlin 2.x.
# Many Flutter plugins still target JVM 1.8 while the app targets 11; this downgrades
# the check to a warning so builds are not blocked while plugins update.
kotlin.jvm.target.validation.mode=IGNORE
